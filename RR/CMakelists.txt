# RR 层：Rachford–Rice 闪蒸

file(GLOB RR_SOURCES
     "${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp"
)
file(GLOB RR_HEADERS
     "${CMAKE_CURRENT_SOURCE_DIR}/include/*.hpp"
     "${CMAKE_CURRENT_SOURCE_DIR}/include/*.h"
)

add_library(rrflash STATIC
    ${RR_SOURCES}
    ${RR_HEADERS}
)

target_include_directories(rrflash
    PUBLIC
      ${CMAKE_CURRENT_SOURCE_DIR}/include
      ${CMAKE_CURRENT_SOURCE_DIR}/../Thermo/include
      ${CMAKE_CURRENT_SOURCE_DIR}/../PhaseStability/include
)

# RR 需要 thermo 和 phase_stability 库
target_link_libraries(rrflash
    PUBLIC
      thermo
      phase_stability
      Eigen3::Eigen
)

# ===================== RR 测试 =====================

add_executable(RRflash_test
    tests/RRflash_test.cpp
)

target_include_directories(RRflash_test
    PRIVATE
      ${CMAKE_CURRENT_SOURCE_DIR}/include
      ${CMAKE_CURRENT_SOURCE_DIR}/../Thermo/include
)

target_link_libraries(RRflash_test
    PRIVATE
      rrflash
      thermo
      GTest::gtest
      GTest::gtest_main
)

# ===================== VLL 三相测试 =====================

add_executable(RR_vll_test
    tests/RR_vll_test.cpp
)

target_include_directories(RR_vll_test
    PRIVATE
      ${CMAKE_CURRENT_SOURCE_DIR}/include
      ${CMAKE_CURRENT_SOURCE_DIR}/../Thermo/include
      ${CMAKE_CURRENT_SOURCE_DIR}/../PhaseStability/include
)

target_link_libraries(RR_vll_test
    PRIVATE
      rrflash
      thermo
      phase_stability
      GTest::gtest
      GTest::gtest_main
)
